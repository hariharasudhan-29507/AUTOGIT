import { motion } from 'motion/react'
import type { ButtonHTMLAttributes, ReactNode } from 'react'
export type SpecularButtonProps = ButtonHTMLAttributes<HTMLButtonElement> & { children?: ReactNode; size?: 'sm' | 'md' | 'lg'; radius?: number; tint?: string; tintOpacity?: number; blur?: number; textColor?: string; lineColor?: string; baseColor?: string; intensity?: number; shineSize?: number; shineFade?: number; thickness?: number; speed?: number; followMouse?: boolean; proximity?: number; autoAnimate?: boolean }
export default function SpecularButton({ children = 'Get Started', size = 'lg', radius = 18, tint = '#ffffff', tintOpacity = 0, blur = 0, textColor = '#f5f5f5', lineColor = '#ffffff', baseColor = '#525252', intensity = 1, shineSize = 10, shineFade = 40, thickness = 1, speed = .35, followMouse = true, proximity = 250, autoAnimate = false, className = '', style, ...props }: SpecularButtonProps) { const padding = size === 'lg' ? 'px-7 py-3.5 text-base' : size === 'sm' ? 'px-4 py-2 text-xs' : 'px-5 py-2.5 text-sm'; return <motion.div whileHover={{ scale: 1.025 }} whileTap={{ scale: .97 }} className="inline-flex"><button className={`specular-button relative inline-flex items-center justify-center overflow-hidden border font-semibold focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--ag-focus)] focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-55 ${padding} ${className}`} style={{ '--sb-radius': `${radius}px`, '--sb-tint': tint, '--sb-tint-opacity': tintOpacity, '--sb-blur': `${blur}px`, '--sb-text': textColor, '--sb-line': lineColor, '--sb-base': baseColor, '--sb-intensity': intensity, '--sb-speed': `${Math.max(.1, speed)}s`, '--sb-shine': `${shineSize + shineFade}deg`, '--sb-thickness': `${thickness}px`, '--sb-proximity': `${proximity}px`, ...style } as React.CSSProperties} data-follow-mouse={followMouse} data-auto-animate={autoAnimate} {...props}><span className="specular-button__sheen" aria-hidden="true" /><span className="relative z-10">{children}</span></button></motion.div> }