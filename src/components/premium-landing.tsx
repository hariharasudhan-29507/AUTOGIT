import { motion, useReducedMotion } from 'motion/react'
import { ArrowRight, Info } from 'lucide-react'
import { Link } from 'react-router-dom'
import Beams from '@/components/beams'
import SpecularButton from '@/components/specular-button'
export function PremiumLanding() { const reducedMotion = useReducedMotion(); return <main className="min-h-[calc(100vh-80px)] bg-black"><section className="relative isolate flex min-h-[calc(100vh-80px)] items-center justify-center overflow-hidden px-5 py-16"><div className="absolute inset-0 bg-[radial-gradient(ellipse_at_50%_42%,rgba(255,255,255,.11),transparent_24%),linear-gradient(#000,#000)]" />{!reducedMotion && <Beams beamWidth={2} beamHeight={18} beamNumber={12} lightColor="#ffffff" speed={.35} noiseIntensity={1.75} scale={.2} rotation={-28} />}<motion.div initial={reducedMotion ? false : { opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .55 }} className="relative z-10 mx-auto max-w-2xl text-center"><div className="mx-auto inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1.5 font-mono text-[10px] uppercase tracking-[.16em] text-white/60"><span className="rounded-full bg-white px-2 py-1 text-[9px] font-semibold text-black">NEW</span> Repository operating system</div><h1 className="mt-7 font-display text-5xl font-semibold leading-[.94] tracking-[-.065em] text-white sm:text-7xl">Repository work,<br />made <span className="text-white/55">radiant.</span></h1><p className="mx-auto mt-6 max-w-lg text-base leading-7 text-white/55">Secure context for every repository decision. Connect GitHub, understand the signal, and act only on real data.</p><div className="mt-9 flex flex-col justify-center gap-3 sm:flex-row"><Link to="/signup"><SpecularButton size="lg" tint="#ffffff" textColor="#09090b" lineColor="#ffffff">Get started <ArrowRight className="ml-2 size-4" /></SpecularButton></Link><Link to="/about" className="inline-flex h-13 items-center justify-center gap-2 rounded-2xl border border-white/10 bg-white/[.04] px-6 text-sm font-semibold text-white/70 hover:bg-white/10 hover:text-white"><Info className="size-4" />Learn more</Link></div></motion.div></section></main> }